What is the main purpose of generics in Java?;To provide compile-time type safety;To reduce code size;To allow reflection;To provide compile-time type safety;3

Which of the following is a correct declaration of a generic class?;generic Box<T> { };Box<T> class { };class Box<T> { };class Box<T> { };3

What does <T> represent in a generic class or method?;A placeholder for a type;A primitive type;A constant;A placeholder for a type;3

Which of the following is NOT a benefit of generics?;Type safety;Code reusability;Runtime casting;Runtime casting;3

Which of these statements will cause a compile-time error?;List<Object> list = new ArrayList<String>();Map<Integer, String> map = new HashMap<>();Set<Double> set = new HashSet<>();List<Object> list = new ArrayList<String>();3

Which of the following is an interface in the collections framework?;List;ArrayList;HashSet;List;3

Which class allows duplicate elements and maintains insertion order?;HashSet;ArrayList;HashMap;ArrayList;3

What is the key difference between ArrayList and LinkedList?;ArrayList is synchronized;LinkedList stores data in contiguous memory;LinkedList allows faster insertion/removal in the middle;LinkedList allows faster insertion/removal in the middle;3

Which collection would you use to store unique elements only?;ArrayList;HashSet;Vector;HashSet;3

Which of these is NOT thread-safe by default?;ArrayList;Hashtable;ConcurrentHashMap;ArrayList;3